import { type ContentConfig } from '@/types/config';

export const contentConfig: ContentConfig = {
  itemsPerPage: 12,
  memoriesPerChapter: 20,
  showDates: true,
  showMood: true,
  showTags: true,
  defaultSortBy: 'date',
  defaultSortOrder: 'desc',
};
